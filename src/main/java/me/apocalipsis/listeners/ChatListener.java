package me.apocalipsis.listeners;

import org.bukkit.event.Listener;

/**
 * ChatListener removido: el sistema /chat y su formateo/moderación
 * han sido deshabilitados. Esta clase permanece vacía para compatibilidad
 * de compilación mientras se eliminan referencias en el proyecto.
 */
public class ChatListener implements Listener {
    // Intencionalmente vacío
}
